/**
 * Defines an object that includes a length, like a string or an array.
 */
export interface IncludesLength {
    length: number
}

/**
 * Defines an object that includes a size, like a Set or a Map.
 */
export interface IncludesSize {
    size: number
}
